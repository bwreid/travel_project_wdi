<nav class="top-bar">
  <ul class="title-area">
    <!-- Title Area -->
    <li class="name">
      <h1><%= link_to("DU Trips", root_path) %></a></h1>
    </li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Left Nav Section -->
    <ul class="left">
      <li class="divider"></li>
      <li><%= link_to("My Itineraries", itineraries_path) %>
      <li class="divider"></li>
    </ul>

    <!-- Right Nav Section -->
    <ul class="right">
      <li class="divider"></li>
      <% if @auth.present? %>
      <li><%= link_to("My Account", @auth) %></li>
      <li class="divider"></li>
      <li><%= link_to("Logout", login_path, :method => 'delete') %></li>
      <li class="divider"></li>
      <% end %>
      <% if @auth.nil? %>
      <li><%= link_to("Create Account", new_user_path) %></li>
      <li class="divider"></li>
      <li><%= link_to("Login", login_path) %></li>
      <li class="divider"></li>
      <% end %>
    </ul>
  </section>
</nav>